const Footer = () => (
  <footer className="border-t border-border py-8">
    <div className="container mx-auto px-4 flex items-center justify-center gap-1.5">
      <span className="text-sm text-muted-foreground">Desenvolvido por</span>
      <span className="text-sm font-bold text-foreground" style={{ fontFamily: "'Petrona', serif" }}>
        CYRIUS
      </span>
    </div>
  </footer>
);

export default Footer;
